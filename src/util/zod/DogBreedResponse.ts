import { z } from 'zod';

const DogBreedResponse = z.object({
  message: z.object({
    affenpinscher: z.array(z.string()),
    african: z.array(z.string()),
    airedale: z.array(z.string()),
    akita: z.array(z.string()),
    appenzeller: z.array(z.string()),
    australian: z.array(z.string()),
    basenji: z.array(z.string()),
    beagle: z.array(z.string()),
    bluetick: z.array(z.string()),
    borzoi: z.array(z.string()),
    bouvier: z.array(z.string()),
    boxer: z.array(z.string()),
    brabancon: z.array(z.string()),
    briard: z.array(z.string()),
    buhund: z.array(z.string()),
    bulldog: z.array(z.string()),
    bullterrier: z.array(z.string()),
    cattledog: z.array(z.string()),
    chihuahua: z.array(z.string()),
    chow: z.array(z.string()),
    clumber: z.array(z.string()),
    cockapoo: z.array(z.string()),
    collie: z.array(z.string()),
    coonhound: z.array(z.string()),
    corgi: z.array(z.string()),
    cotondetulear: z.array(z.string()),
    dachshund: z.array(z.string()),
    dalmatian: z.array(z.string()),
    dane: z.array(z.string()),
    deerhound: z.array(z.string()),
    dhole: z.array(z.string()),
    dingo: z.array(z.string()),
    doberman: z.array(z.string()),
    elkhound: z.array(z.string()),
    entlebucher: z.array(z.string()),
    eskimo: z.array(z.string()),
    finnish: z.array(z.string()),
    frise: z.array(z.string()),
    germanshepherd: z.array(z.string()),
    greyhound: z.array(z.string()),
    groenendael: z.array(z.string()),
    havanese: z.array(z.string()),
    hound: z.array(z.string()),
    husky: z.array(z.string()),
    keeshond: z.array(z.string()),
    kelpie: z.array(z.string()),
    komondor: z.array(z.string()),
    kuvasz: z.array(z.string()),
    labradoodle: z.array(z.string()),
    labrador: z.array(z.string()),
    leonberg: z.array(z.string()),
    lhasa: z.array(z.string()),
    malamute: z.array(z.string()),
    malinois: z.array(z.string()),
    maltese: z.array(z.string()),
    mastiff: z.array(z.string()),
    mexicanhairless: z.array(z.string()),
    mix: z.array(z.string()),
    mountain: z.array(z.string()),
    newfoundland: z.array(z.string()),
    otterhound: z.array(z.string()),
    ovcharka: z.array(z.string()),
    papillon: z.array(z.string()),
    pekinese: z.array(z.string()),
    pembroke: z.array(z.string()),
    pinscher: z.array(z.string()),
    pitbull: z.array(z.string()),
    pointer: z.array(z.string()),
    pomeranian: z.array(z.string()),
    poodle: z.array(z.string()),
    pug: z.array(z.string()),
    puggle: z.array(z.string()),
    pyrenees: z.array(z.string()),
    redbone: z.array(z.string()),
    retriever: z.array(z.string()),
    ridgeback: z.array(z.string()),
    rottweiler: z.array(z.string()),
    saluki: z.array(z.string()),
    samoyed: z.array(z.string()),
    schipperke: z.array(z.string()),
    schnauzer: z.array(z.string()),
    segugio: z.array(z.string()),
    setter: z.array(z.string()),
    sharpei: z.array(z.string()),
    sheepdog: z.array(z.string()),
    shiba: z.array(z.string()),
    shihtzu: z.array(z.string()),
    spaniel: z.array(z.string()),
    spitz: z.array(z.string()),
    springer: z.array(z.string()),
    stbernard: z.array(z.string()),
    terrier: z.array(z.string()),
    tervuren: z.array(z.string()),
    vizsla: z.array(z.string()),
    waterdog: z.array(z.string()),
    weimaraner: z.array(z.string()),
    whippet: z.array(z.string()),
    wolfhound: z.array(z.string()),
  }),
  status: z.string(),
});

export default DogBreedResponse;
